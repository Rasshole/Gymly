{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/auth/callback*", "/confirm*", "/auth/confirm*", "/.well-known/*", "/assets/*", "/*.{css,scss,js,png,gif,ico,jpg,jpeg,svg,webp,woff,woff2}"]
  }
}
