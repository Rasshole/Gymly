/**
 * Gymly Theme Configuration
 */

import colors from './colors';
import {spacing, radius, shadows, typography} from './designTokens';

export const theme = {
  colors,
  spacing,
  borderRadius: radius,
  radius,
  typography,
  shadows,
};

export default theme;

