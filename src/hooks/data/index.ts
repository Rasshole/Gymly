/**
 * Data hooks – use these in screens instead of direct mock imports
 */

export {useActivityData} from '../useActivityData';
export {useGroups, useJoinedGroups, useGroup, useGroupActivity} from '../useGroupData';
export {useOnlineUsers} from '../useOnlineUsers';
export {
  useProfileStats,
  useProfileBadges,
  useProfileDisplay,
  useWeeklyStats,
  useMilestones,
} from '../useProfileData';
